# Production build env — points the Next.js web app at the Render-hosted
# API. The host comes online when the render.yaml Blueprint is activated
# and a DATABASE_URL is supplied for a managed Postgres.
NEXT_PUBLIC_API_BASE_URL=https://job-flow-api.onrender.com/api
