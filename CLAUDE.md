# Norman Handyman MVP

Two-sided handyman business app for Norman, Oklahoma.

## Architecture

| Layer | Stack | Dir |
|-------|-------|-----|
| Backend | Django 5.1 + DRF + PostgreSQL | `/backend` |
| Customer Web | Next.js 15 (App Router, TypeScript, Tailwind) | `/web` |
| Operator Mobile | Expo (React Native, TypeScript, expo-router) | `/mobile` |

## Quick Start

```bash
# 1. Database
docker compose up -d

# 2. Backend (terminal 1)
cd backend
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env   # Edit Stripe keys
python manage.py migrate
python manage.py seed_demo    # Creates operator user + demo data
python manage.py runserver

# 3. Web (terminal 2)
cd web && npm install && npm run dev

# 4. Mobile (terminal 3)
cd mobile && npm install && npx expo start

# 5. Stripe webhooks (terminal 4)
stripe listen --forward-to localhost:8000/api/webhooks/stripe/
```

## Env Vars

### backend/.env
- `DJANGO_SECRET_KEY` — change in production
- `DB_NAME`, `DB_USER`, `DB_PASSWORD`, `DB_HOST`, `DB_PORT` — Postgres connection
- `STRIPE_SECRET_KEY` — `sk_test_...`
- `STRIPE_WEBHOOK_SECRET` — `whsec_...` (from `stripe listen` output)
- `EMAIL_BACKEND` — `django.core.mail.backends.console.EmailBackend` (dev)
- `CORS_ALLOWED_ORIGINS` — `http://localhost:3000,http://localhost:8081`

### web/.env.local
- `NEXT_PUBLIC_API_BASE_URL` — `http://localhost:8000/api`

### mobile/.env
- `EXPO_PUBLIC_API_BASE_URL` — `http://localhost:8000/api` (emulator) or `http://<LAN-IP>:8000/api` (device)

## Data Model

`Customer` → `BookingRequest` → `Job` → `Estimate` / `Invoice` → Stripe payment

Statuses:
- BookingRequest: NEW → CONTACTED → SCHEDULED → CLOSED
- Job: SCHEDULED → IN_PROGRESS → COMPLETED → CANCELED
- Estimate: DRAFT → SENT → APPROVED → REJECTED
- Invoice: DRAFT → SENT → PAID → OVERDUE → VOID

## API

Auth: DRF TokenAuthentication. Get token: `POST /api-token-auth/` with `{username, password}`.

### Public (no auth)
- `POST /api/booking-requests/public/` — submit booking
- `GET /api/invoices/public/{id}/` — view invoice
- `POST /api/invoices/public/{id}/pay/` — create Stripe checkout

### Operator (auth required)
- `/api/customers/`, `/api/booking-requests/`, `/api/jobs/`, `/api/estimates/`, `/api/invoices/`, `/api/expenses/`, `/api/supplies/`
- Custom actions: `convert_to_job`, `start`, `complete`, `send_estimate`, `send_invoice`, `mark_paid`, `create_checkout`

### Webhooks
- `POST /api/webhooks/stripe/` — `checkout.session.completed` → marks invoice PAID

## Golden Path Demo Script

1. Start all services (backend, web, mobile, stripe listen)
2. **Customer submits booking** → web `/book` form → confirmation email logged
3. **Operator sees request** → mobile Today tab → tap booking → "Convert to Job"
4. **Operator creates invoice** → Job detail → "Create Invoice" → add line items → "Create & Share Link"
5. **Customer pays** → web `/pay/{id}` → "Pay Now" → Stripe test card `4242 4242 4242 4242`
6. **Webhook fires** → invoice flips to PAID → confirmation email logged
7. **Operator sees PAID** → mobile Invoices tab shows green PAID badge

## Known Pitfalls

- **Android device**: use LAN IP not `localhost` for API URL
- **Stripe test card**: `4242 4242 4242 4242`, any future date, any CVC
- **Emails in dev**: console backend logs to terminal, not actually sent
- **CORS**: if web can't reach backend, check `CORS_ALLOWED_ORIGINS`
- **Token auth**: mobile stores token in AsyncStorage; clear app data to reset

## Project Context

- **Workspace**: managed via Donkey Betz platform PA (Rigby), workspace ID `6f692cba-c436-4ac1-a4d3-5f16c576dae8`
- **Build Packet**: generated by PA, stored as deliverables in the workspace
- **Brand**: Navy (#0B1F3A) / Steel Blue (#2C5E8A) / Gold (#C9A227) palette, targeting 45+ homeowners


<!-- context-kit:adopt:start -->

## Project facts (auto-detected)

_Generated by `context-kit adopt` on 2026-05-21. Re-runs update this block in place._

- **Project:** Norman Handyman Mvp
- **Stack:**
  - **Backend:** Python (detected from `backend/manage.py`)
  - **Web:** JavaScript / Node.js (detected from `web/package.json`)
  - **Mobile:** JavaScript / Node.js (detected from `mobile/package.json`)
- **Manifests seen:** `backend/manage.py`, `backend/requirements.txt`, `web/package.json`, `mobile/package.json`

## Adopt Summary

**Type:** Full-stack web app (medium)

**Structure:**

- No workspace child projects detected.

**Reality:** Mixed workspace project (confidence: Medium). _Recognized subdirs (backend, mobile, web) carry distinct stacks (JavaScript / Node.js, Python)._

**Next actions (2):**

- **[medium] Confirm backend/frontend boundaries** — Backend and frontend code appear to live in separate workspaces. Confirm API ownership, local dev startup order, and deployment boundaries.
- **[medium] Classify unrecognized directories** — Confirm the role of: docs/.

## Agent launch prompt

Copy-paste this into your AI coding agent (Claude Code, Cursor, Aider, etc.) as the first message. Confidence: medium.

```text
You are working in a project that context-kit just adopted. Before writing any code, do the following.

WHAT THIS PROJECT APPEARS TO BE
Full-stack web app (medium confidence)
Reasoning: Recognized split monorepo with a Python backend (backend/) and a JavaScript frontend (web/).

USER CONTEXT (provided during adopt)
- Project (per the user): A complete service-business operating layer for handyman, trade, and small-team service operators (the JobFlow product). Customers book through a public web form with photos and scheduling; operators dispatch and execute jobs on a React Native mobile app; Stripe handles checkout, estimates, and invoices end-to-end. Expense and supply inventory tracking included.
- Next task (per the user): De-stub Stripe and exit the Render free tier so the live deploy at job-flow-iota.vercel.app stops cold-starting after 15-minute idle.

PRIMARY DETECTION
Split monorepo — backend=Python, web=JavaScript / Node.js, mobile=JavaScript / Node.js

WORKSPACE / PROJECT STRUCTURE
- No workspace child projects detected.

RECOMMENDED FIRST ACTION
Inspect the backend/frontend boundary first.
- Read the backend code to understand the API surface, models, and ownership.
- Read the frontend workspace to see how it consumes the backend.
- Confirm the local-dev startup order (which side starts first, on what ports) and the deployment boundary.

HOW TO APPROACH THIS REPO
If the project structure is not immediately clear, or the confidence shown above is low, do a structured read-through before proposing tasks: start with entry points (README, main files, routing, configs), map the system (API, frontend, integrations, data flow), and call out inconsistencies, risks, or missing pieces you find.

If confidence is medium, do a quick inspection first — read the README, the top-level structure, and one or two main files — then decide whether the project needs deeper analysis or whether you can move directly to a concrete first task.

If the structure IS clear and confidence is high, skip the read-through and go directly to identifying the highest-value next task — don't pad the response with unnecessary inspection steps.

WHAT TO PRIORITIZE
Prefer identifying real risks, inconsistencies, missing wiring, or unused / incomplete features over surface-level tasks. Do not default to documentation updates unless the user explicitly asked for them.

SAFETY INSTRUCTIONS
- If generated docs exist, read BUILD_PLAN.md, PROJECT_WHAT_IT_IS.md, and CLAUDE.md before making changes. If they do not exist yet, read README, package/manifests, and inspect the detected project structure first.
- Do not change the project's stack (e.g. swapping frameworks, languages, or build tools) without asking the user first.
- Do not run destructive commands (rm -rf, force-push, drop database, schema deletes, etc.).
- Ask before broad refactors that touch more than 3-5 files.
- If the generated docs are missing, do not treat that as an error; continue with repo inspection and propose the first safe task.

TO PRESERVE THIS CONTEXT
After your initial inspection, end your response with a short copy-paste command the user can run to persist this context:

  context-kit adopt . --write \
    --project-summary "<refined one-sentence project summary>" \
    --next-task "<recommended next task>" \
    --notes "<key findings from this inspection>"

When filling in the placeholders:
- Keep notes concise but specific. Include real risks, missing wiring, stale docs, dirty git state, or incomplete features.
- Do not include secrets, tokens, or credentials in any value.
- Escape quotes (\") inside values, or rephrase to avoid quotes.
- If no meaningful findings exist, omit --notes.
- Do not run the command yourself — emit it for the user to copy.

When you're ready to begin work, summarize what you read and propose a concrete first task.
```

### Needs clarification

These directories contain files adopt can see, but it cannot
confidently identify their role yet. Confirm what they are
before making changes.


**Data / content / non-code directories (1):** `docs/`

_These contain files but none with extensions adopt categorizes as source (likely config / data / documentation). Listed compactly so the high-signal directories above stay visible._

### What the user told adopt

- **What this project is:** A complete service-business operating layer for handyman, trade, and small-team service operators (the JobFlow product). Customers book through a public web form with photos and scheduling; operators dispatch and execute jobs on a React Native mobile app; Stripe handles checkout, estimates, and invoices end-to-end. Expense and supply inventory tracking included.
- **What's next:** De-stub Stripe and exit the Render free tier so the live deploy at job-flow-iota.vercel.app stops cold-starting after 15-minute idle.

### Rule for this session

- Always read `docs/BUILD_PLAN.md` before choosing a stack or writing
  code. The detected stack above is the source of truth — do not
  switch frameworks without asking.
- `[adopt: please describe]` placeholders mark context the user
  hasn't filled in yet. **Do not block read-only inspection on them.**
  Proceed with the recommended first action above if it's safe and
  read-only. Only ask the user before making decisions (writes,
  refactors, framework choices) that depend on the missing context.

<!-- context-kit:adopt:end -->
